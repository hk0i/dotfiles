#!/usr/bin/env python3

import csv
import sys


def print_chapter(row):
    print(f'{row["Source In"][:-3]} - {row["Notes"]}')


def scan_rows(input):
    csv_input = csv.DictReader(input)
    for row in csv_input:
        print_chapter(row)


if __name__ == '__main__':
    scan_rows(sys.stdin)
